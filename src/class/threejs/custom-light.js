//import * as THREE from "three";

export class CustomLight {
  /**
   * It takes a vector as an argument and sets the position of the particle to that vector
   * @param vector - The position of the object.
   */
  constructor(vector) {
    this.position = vector;
  }
}
